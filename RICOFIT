<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Registro de Clientes - RicoFit75</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4faff;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    h1 {
      background-color: #cce5f7;
      color: #000;
      padding: 15px;
      z-index: 2;
      position: relative;
    }

    form {
      background: rgba(255, 255, 255, 0.95);
      width: 350px;
      margin: 30px auto;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      position: relative;
      z-index: 2;
    }

    label {
      display: block;
      text-align: left;
      margin: 10px 0 5px;
      font-weight: bold;
    }

    input[type="text"], input[type="date"], input[type="number"], select {
      width: 100%;
      padding: 8px;
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    .botones {
      margin-top: 20px;
      display: flex;
      justify-content: space-around;
    }

    button {
      background: linear-gradient(to bottom, #6dcff6, #007bbd);
      border: none;
      color: white;
      padding: 10px 20px;
      font-size: 16px;
      border-radius: 6px;
      cursor: pointer;
      box-shadow: 3px 3px 6px rgba(0,0,0,0.2);
    }

    button:hover {
      background: linear-gradient(to bottom, #4ec0f0, #005f99);
    }

    /* Logo grande al fondo */
    .logo-fondo {
      position: absolute;
      top: 50%;
      left: 50%;
      width: 600px;
      opacity: 0.08;
      transform: translate(-50%, -50%);
      z-index: 1;
    }
  </style>
</head>
<body>

  <h1>REGISTRO DE CLIENTES</h1>

  <img src="logo.png" alt="RicoFit75" class="logo-fondo">

  <form id="formRegistro">
    <label>Nombre cliente</label>
    <input type="text" id="nombre">

    <label>Fecha</label>
    <input type="date" id="fecha">

    <label>Monto</label>
    <input type="number" id="monto">

    <label>Tipo de pago</label>
    <select id="pago">
      <option value="">Selecciona una opción</option>
      <option value="Diario">Diario</option>
      <option value="Semanal">Semanal</option>
      <option value="Mensual">Mensual</option>
    </select>

    <div class="botones">
      <button type="button" onclick="buscar()">BUSCAR</button>
      <button type="button" onclick="ingresar()">INGRESA</button>
      <button type="reset">LIMPIAR</button>
    </div>
  </form>

  <!-- Firebase -->
  <script type="module">
    // Importar Firebase
    import { initializeApp } from "https://www.gstatic.com/firebasejs/12.4.0/firebase-app.js";
    import { getDatabase, ref, push, set } from "https://www.gstatic.com/firebasejs/12.4.0/firebase-database.js";

    const firebaseConfig = {
      apiKey: "AIzaSyBF_X8ZZnDhP2bimzzC9sL6VkYR1a4cln8",
      authDomain: "ricofit75-4bce0.firebaseapp.com",
      databaseURL: "https://ricofit75-4bce0-default-rtdb.firebaseio.com",
      projectId: "ricofit75-4bce0",
      storageBucket: "ricofit75-4bce0.firebasestorage.app",
      messagingSenderId: "648414560232",
      appId: "1:648414560232:web:d303ad8b3ab439cd7aae5d",
      measurementId: "G-1LY44KPELW"
    };

    // Inicializar Firebase
    const app = initializeApp(firebaseConfig);
    const db = getDatabase(app);

    // Función para ingresar datos
    window.ingresar = function() {
      const nombre = document.getElementById('nombre').value.trim();
      const fecha = document.getElementById('fecha').value;
      const monto = document.getElementById('monto').value;
      const pago = document.getElementById('pago').value;

      if (!nombre || !fecha || !monto || !pago) {
        alert("Por favor completa todos los campos");
        return;
      }

      const nuevoCliente = {
        nombre: nombre,
        fecha: fecha,
        monto: monto,
        pago: pago
      };

      // Guardar en Firebase
      const referencia = ref(db, 'clientes/');
      const nuevoRegistro = push(referencia);
      set(nuevoRegistro, nuevoCliente)
        .then(() => {
          alert("✅ Cliente registrado correctamente");
          document.getElementById("formRegistro").reset(); // limpia el formulario
        })
        .catch((error) => {
          alert("❌ Error al guardar: " + error.message);
        });
    };

    // Función búsqueda (placeholder)
    window.buscar = function() {
      alert("Función de búsqueda en desarrollo 😅");
    };
  </script>
</body>
</html>
